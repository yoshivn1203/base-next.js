// export const position = {
//   relative: css`
//     position: relative;
//     left: ${({ left }) => left}px;
//     top: ${({ top }) => top}px;
//     right: ${({ right }) => right}px;
//     bottom: ${({ bottom }) => bottom}px;
//   `,
//   absolute: css`
//     position: relative;
//     left: ${({ left }) => left}px;
//     top: ${({ top }) => top}px;
//     right: ${({ right }) => right}px;
//     bottom: ${({ bottom }) => bottom}px;
//   `,
// };
